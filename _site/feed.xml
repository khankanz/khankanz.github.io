<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="http://0.0.0.0:4000/feed.xml" rel="self" type="application/atom+xml" /><link href="http://0.0.0.0:4000/" rel="alternate" type="text/html" /><updated>2026-01-19T18:30:05-05:00</updated><id>http://0.0.0.0:4000/feed.xml</id><title type="html">Zardar Khan</title><subtitle>Applied AI Engineer specializing in clinical NLP systems. Production LLM pipelines in healthcare.</subtitle><author><name>Zardar Khan</name></author><entry><title type="html">Building a ML Pipeline for Microcalcification Classification in OMOP</title><link href="http://0.0.0.0:4000/2025/10/30/ml-pipeline.html" rel="alternate" type="text/html" title="Building a ML Pipeline for Microcalcification Classification in OMOP" /><published>2025-10-30T00:00:00-04:00</published><updated>2025-10-30T00:00:00-04:00</updated><id>http://0.0.0.0:4000/2025/10/30/ml-pipeline</id><content type="html" xml:base="http://0.0.0.0:4000/2025/10/30/ml-pipeline.html"><![CDATA[<p>This post is a detailed system design walkthrough for an NLP pipeline built to classify microcalcification status from radiology reports and persist the findings into an OMOP Common Data Model. Starting with no existing infrastructure, I’ll break down the key technical challenges and implementation details. We’ll cover everything from establishing a robust, transactional database foundation to solving for non-auto-incrementing primary keys in a concurrent environment, dynamically routing NLP results to the correct OMOP domain tables, and using the <code class="language-plaintext highlighter-rouge">fact_relationship</code> table to ensure data linkage and discoverability in tools like Atlas.</p>

<hr />

<h2 id="introduction">Introduction</h2>
<p>Today, I’m going to walk you through the system design and implementation of an ML pipeline I built for classifying microcalcification status from radiology reports. This was one of my initial tasks, starting from zero plumbing. We read radiology reports from an Aidbox database, where they’re stored as observations linked to diagnostic reports of the FHIR resource. We then use a trained DeBERTa microcalcifications model to classify the status, and save the results into an OMOP database. This involved solving multiple challenges at each step—I’ll focus on the key investigations and implementations I handled. Let’s dive into the pipeline end-to-end.</p>

<h2 id="database-connection-and-setup">Database Connection and Setup</h2>
<p>First, we connect to a Postgres database. I created environment files that the script reads initially—these contain database connection variables. This allows the same code to run on testing or production systems.</p>

<p>We have helper functions to test the connection and ensure it’s successful. There’s also setup for logging, saving all actions to a log file. We’re using SQLAlchemy as a Pythonic wrapper around database controls. Logging is done at the database connection level, so every read-write operation gets logged—any transactions on the database are captured.</p>

<p>Next, we use another library called FastAI, which provides a simplifying wrapper around the database. We set <code class="language-plaintext highlighter-rouge">DBM = DB.meta</code> to get the metadata of the database. This lets us do things like <code class="language-plaintext highlighter-rouge">DBM.tables</code> to get all table names, and reflect the structure. Reflection means creating Pythonic objects (using Pydantic or SQLAlchemy) that represent the database structure. For example, a person is represented as <code class="language-plaintext highlighter-rouge">P = DBM.person</code>.</p>

<p>We define a context manager via <code class="language-plaintext highlighter-rouge">contextlib</code>. There’s also a helper function for Year, Month, Day, which is handy for dealing with Aidbox. This gives us a data model representing valid columns, like in the persons table.</p>

<p>We create a <code class="language-plaintext highlighter-rouge">dbTable</code> object, which includes the connection to the actual table, the database object, and the data model (e.g., for person). The context manager allows transactions on tables: we try the transaction, and if there’s an exception, we roll back to avoid corrupt data. This lets us query like “Find me a person where person source value or pid is equal to xyz” and retrieve the person.</p>

<h2 id="handling-primary-keys-no-auto-increment">Handling Primary Keys (No Auto-Increment)</h2>
<p>There’s a weird situation with OMOP tables where the next primary key isn’t auto-incremented. I investigated and wrote a helper function to get the next primary key for a given table. It grabs the first primary key column, runs a SQL query to check the maximum value in that column. If nothing is returned or it’s 0, next ID is 1; otherwise, it’s the max plus 1.</p>

<p>But there was a bug: with batch calls and multiple processes, we might get the same max ID. My workaround was to create <code class="language-plaintext highlighter-rouge">get next primary key range</code>—same process, but it gets a range based on how many you need to create. You use that range to populate IDs when creating observations.</p>

<h2 id="adding-persons-and-basic-records">Adding Persons and Basic Records</h2>
<p>The first functionality I built was to add a person. Given person data and the persons table, it checks if there’s an existing patient by matching on <code class="language-plaintext highlighter-rouge">personSourceValue</code>. If there’s a match, return it; otherwise, insert the data.</p>

<p>For notes, there are helper functions for parsing date time, which is a big pain—FHIR date time needs conversion to an OMOP-acceptable format.</p>

<p>There’s a general <code class="language-plaintext highlighter-rouge">fetch records</code> function: given a database table, column name, and where clause, it fetches the record. This generalizes to persons table—for example, fetch by <code class="language-plaintext highlighter-rouge">person source value</code> (the FHIR ID). I used Python partials to make it cooler: set the persons table and <code class="language-plaintext highlighter-rouge">person source value</code> as the column, then just provide the ID to create <code class="language-plaintext highlighter-rouge">fetch patients</code>.</p>

<h2 id="creating-notes">Creating Notes</h2>
<p>Creating a note starts with prepping the note object. You have the source value (a string), effective start date, patient ID, and note table. Parse the effective start date from the diagnostic report resource into date and time.</p>

<p>Return a note object: note ID from <code class="language-plaintext highlighter-rouge">get next primary key</code>, person ID as PID, note date as whatever. Key fields: <code class="language-plaintext highlighter-rouge">note type concept ID</code> is <code class="language-plaintext highlighter-rouge">type concept ID.EHR</code>; <code class="language-plaintext highlighter-rouge">Note Class Concept ID</code> is <code class="language-plaintext highlighter-rouge">Note Class Concept ID.diagnostic study</code>; <code class="language-plaintext highlighter-rouge">Note Text</code> is the source value. We agreed <code class="language-plaintext highlighter-rouge">Note Text</code> contains the FHIR Diagnostic Report ID.</p>

<p>Once prepped, add the note using a wrapper helper. Roll this into <code class="language-plaintext highlighter-rouge">create and add the note</code>—it’s an iterative buildup of functions.</p>

<h2 id="concept-table-lookups-and-caching">Concept Table Lookups and Caching</h2>
<p>For the concept table, we need a function to take a term and look up details. Connect to the concept table as <code class="language-plaintext highlighter-rouge">concepts</code> (a DB table object). Search by concept name (e.g., “micro calcifications”) to get SNOMED code or concept ID.</p>

<p>I put a cache around this lookup function because scanning the table takes time, and you won’t look up the same concept many times—caching keeps it in memory to save computation.</p>

<p>The <code class="language-plaintext highlighter-rouge">getConceptDetails</code> function lets you specify vocabulary as <code class="language-plaintext highlighter-rouge">vocabulary type SNOMED</code> and concept class (none or something else). It searches for concept name like the term, where standard concept is ‘s’ and vocabulary ID is SNOMED. Returns the first match as a dictionary (e.g., for “microcalcification”: concept ID 4132707, name “microcalcification”; for “negative”: concept ID 9189, name “negative”). If no match, raise ValueError.</p>

<p><code class="language-plaintext highlighter-rouge">processNlpFinding</code> takes a term (string) and NLP result, returns a dictionary in question-answer format. Map using the concept table: question is from <code class="language-plaintext highlighter-rouge">getConceptDetails</code> on the term; answer is the NLP result. Example: <code class="language-plaintext highlighter-rouge">processNlpFinding('microcalcification unknown')</code> gives a dict with question (concept ID, name) and answer (concept ID, name).</p>

<h2 id="creating-note-nlp-entries">Creating Note NLP Entries</h2>
<p>OMOP is person-centric: person must exist to create a note (pointer back), and note must exist for note NLP (pointer back). Create sequentially.</p>

<p>Connect to note_nlp table as <code class="language-plaintext highlighter-rouge">notes_nlp</code>—understands structure for validation. <code class="language-plaintext highlighter-rouge">FetchNotes</code> is a partial of <code class="language-plaintext highlighter-rouge">fetch records</code> for notes. Fetch a note to get its object (note ID, person ID, date/time, etc.).</p>

<p>Helper: given a note object, extract date (handy for note NLP). To prep note NLP entry: use note date for alignment. Note NLP ID from <code class="language-plaintext highlighter-rouge">get next primary key</code>; note ID points back to note table (one-to-many: one note can have multiple note NLP). For lexical variant (string): process NLP finding, save as JSON (question-answer format). NLP date/time same as note. NLP system is model metadata: “Radbert V1” (manufacturer “EMR TEL Lab”, device name “Radbert Microcalcifications”, version “1.0.0”).</p>

<p>I used Pydantic to extend objects representing tables (e.g., classes for <code class="language-plaintext highlighter-rouge">vocabulary type</code> like SNOMED, <code class="language-plaintext highlighter-rouge">concept ID</code>, <code class="language-plaintext highlighter-rouge">LOINC</code>, <code class="language-plaintext highlighter-rouge">no class concept ID</code>, <code class="language-plaintext highlighter-rouge">type concept ID</code>, language concept like note base class). Pydantic sets rules for valid/optional fields, validates on object creation before database insert.</p>

<p>Before insert, check if note NLP exists for the note (though this might not make sense later—skipping for now). There are batch functions and <code class="language-plaintext highlighter-rouge">create note NLP</code> building on this.</p>

<h2 id="storing-in-domain-tables">Storing in Domain Tables</h2>
<p>Note NLP breaks from OMOP-centric view; to make discoverable (e.g., via Atlas), store in domain tables. Fetch note NLP: get lexical variant (JSON question-answer), look at question concept ID (e.g., for microcalcification: 41327). Fetch concept from concepts table, check domain ID—it tells which OMOP CDM table (e.g., observations for microcalcifications).</p>

<p>Helper: given note NLP ID, return domain type (wraps this logic). Connect to observations table as <code class="language-plaintext highlighter-rouge">observations</code>. Get next primary key. Given note ID, get person ID from associated note.</p>

<p>To prep observation: start with note NLP ID, fetch entry, get findings from lexical variant. Observation ID: next primary key; person ID: from note; observation concept ID: findings question concept ID; date/time: same as note NLP/note; observation type concept ID: NLP (type concept); value as concept: findings answer concept ID; observation source value: “table = note NLP; ID = [note NLP ID]” (for tracking/linkage back).</p>

<h2 id="handling-observation-periods">Handling Observation Periods</h2>
<p>OMOP constraint: patients should be pre-populated with observation period (range of earliest/latest entries). When adding a note, check if note date falls within the patient’s observation period; if not, update start/end date.</p>

<p>I created a class in OMOP CDM 5-4 classes called <code class="language-plaintext highlighter-rouge">ObservationPeriod</code>. Helper: given person ID, retrieve from observation period table, check if note date exceeds range (returns tuple: true/false for too far past/future). Update if needed—otherwise, no validation error, but the entry won’t be discoverable in tools like Atlas (note -&gt; note NLP -&gt; domain table like observation).</p>

<h2 id="using-fact_relationship-for-linkages">Using FACT_RELATIONSHIP for Linkages</h2>
<p>More recently, rather than free text/JSON in observation source value pointing back to note NLP, we use the FACT_RELATIONSHIP table. It allows pointing note NLP to domain table entry (and vice versa) with two concepts: “Has associated procedure” and “Associate procedure of”. For each pointer, add two entries in FACT_RELATIONSHIP for two-way linkage.</p>

<h2 id="wrapping-up">Wrapping Up</h2>
<p>Zooming out: We take a FHIR diagnostic report ID, get text, run through the model (we’ll discuss that), process results into note and note NLP, then propagate to domain tables. This pipeline involved iterative helper functions, caching, transactions, and OMOP linkages I investigated deeply.</p>

<p>Questions? I’d love feedback on what to expand.</p>]]></content><author><name>Zardar Khan</name></author><category term="ml-pipeline" /><category term="omop" /><category term="fhir" /><category term="database-integration" /><category term="nlp" /><category term="radiology" /><category term="sqlalchemy" /><category term="pydantic" /><category term="deberta" /><summary type="html"><![CDATA[A technical walkthrough of an end-to-end ML pipeline that classifies radiology reports and integrates structured findings into an OMOP database.]]></summary></entry><entry><title type="html">On Building Augmented Datasets: A Practical Case Study</title><link href="http://0.0.0.0:4000/2025/09/03/on-augmented-data.html" rel="alternate" type="text/html" title="On Building Augmented Datasets: A Practical Case Study" /><published>2025-09-03T00:00:00-04:00</published><updated>2025-09-03T00:00:00-04:00</updated><id>http://0.0.0.0:4000/2025/09/03/on-augmented-data</id><content type="html" xml:base="http://0.0.0.0:4000/2025/09/03/on-augmented-data.html"><![CDATA[<p>I built a PHI de-identification pipeline that transforms 18,000 proprietary database entries into contextually-rich training data by augmenting real PHI entities with GPT-4 generated medical text. After failing to match real-data performance with purely synthetic data, I pivoted to this hybrid approach.</p>

<p>This isn’t synthetic data generation—it’s data augmentation. The distinction matters.</p>

<hr />

<h2 id="results-first">Results First</h2>

<p>The augmented approach delivered:</p>
<ul>
  <li>F1 score improved from 40% to 75% over 8 iterations</li>
  <li>Model now detects multiple PHI entities per sample (was limited to 1 before)</li>
  <li>Eliminated systematic false positives on common medical terms</li>
</ul>

<hr />

<h2 id="why-augmented-beats-synthetic-the-microcalcification-lesson">Why Augmented Beats Synthetic: The Microcalcification Lesson</h2>

<p>Before this project, I spent months generating synthetic radiology reports for microcalcification classification. Training on 600 real, manually-labeled reports achieved 95% accuracy. I created 6 clusters to identify different report formats, used these as few-shot examples for GPT-4/Gemini, and scaled to 1200 samples.</p>

<p>Despite looking perfect individually, the synthetic-trained model couldn’t break 65% on held-out real data—nowhere close to the 95% from real training data.</p>

<blockquote>
  <p>This failure taught me: <strong>you can’t synthetic-data your way out of a domain shift problem</strong>. Real medical text has idiosyncratic patterns, institutional quirks, and long-tail edge cases that models struggle to generate from scratch.</p>
</blockquote>

<hr />

<h2 id="the-pipeline">The Pipeline</h2>

<h3 id="core-insight-separate-entity-from-context">Core Insight: Separate Entity from Context</h3>

<p>Our proprietary advantage wasn’t just having PHI—it was having <em>real PHI distributions</em>. Account numbers like <code class="language-plaintext highlighter-rouge">A123456</code> appear in specific contexts. Patient IDs follow institutional formats. But GPT-4 can’t see this PHI (privacy constraints), and manual annotation doesn’t scale (took 3 people a week for 2000 samples using Label Studio).</p>

<p>Solution: Templates with placeholders.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">create_phi_template_prompt</span><span class="p">(</span><span class="n">placeholders</span><span class="p">:</span> <span class="nb">list</span><span class="p">[</span><span class="nb">str</span><span class="p">],</span> <span class="n">inspo</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>
    <span class="n">ph</span> <span class="o">=</span> <span class="sh">'</span><span class="s">, </span><span class="sh">'</span><span class="p">.</span><span class="nf">join</span><span class="p">(</span><span class="sa">f</span><span class="sh">'</span><span class="s">&lt;</span><span class="si">{</span><span class="n">p</span><span class="si">}</span><span class="s">&gt;</span><span class="sh">'</span> <span class="k">for</span> <span class="n">p</span> <span class="ow">in</span> <span class="n">placeholders</span><span class="p">)</span>
    <span class="k">return</span> <span class="sa">f</span><span class="sh">"""</span><span class="s"># TASK
Write **one** free‑flowing hospital‑note paragraph (3–8 sentences).

## MUST‑USE PLACEHOLDERS
</span><span class="si">{</span><span class="n">ph</span><span class="si">}</span><span class="s">
(Use each **once**, keep the angle brackets.)

## INSPIRATION
</span><span class="sh">"</span><span class="si">{</span><span class="n">inspo</span><span class="si">}</span><span class="sh">"</span><span class="s">
[... style rules ...]
</span><span class="sh">"""</span>
</code></pre></div></div>

<p>GPT-4 generates text with <code class="language-plaintext highlighter-rouge">&lt;patient_name&gt;</code>, <code class="language-plaintext highlighter-rouge">&lt;encounter_timestamp&gt;</code> placeholders. I fill these locally with real PHI, maintaining security while leveraging LLM creativity.</p>

<h3 id="fighting-repetition-two-noise-sources">Fighting Repetition: Two Noise Sources</h3>

<p>Initially, my 7B model produced variations of “Dr. X discussed the patient’s treatment plan with nursing staff” ad nauseam. Even 72B models disappointed at scale. I needed diversity mechanisms:</p>

<p><strong>1. Random PHI combinations</strong>: Sample 2-N entity types per generation</p>
<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">random_phi_combination</span><span class="p">(</span><span class="n">labels</span><span class="p">,</span> <span class="n">min_n</span><span class="o">=</span><span class="mi">5</span><span class="p">,</span> <span class="n">max_n</span><span class="o">=</span><span class="mi">10</span><span class="p">):</span>
    <span class="k">return</span> <span class="p">[</span><span class="n">random</span><span class="p">.</span><span class="nf">choice</span><span class="p">(</span><span class="n">labels</span><span class="p">)</span> <span class="k">for</span> <span class="n">_</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">random</span><span class="p">.</span><span class="nf">randint</span><span class="p">(</span><span class="n">min_n</span><span class="p">,</span> <span class="n">max_n</span><span class="p">))]</span>
</code></pre></div></div>

<p><strong>2. Inspiration injection</strong>: I filtered the mistral-pii dataset to English-only texts, creating 3200 stylistic seeds. The model must incorporate 2-4 exact phrases, mirror sentence rhythms, but reimagine scenarios clinically.</p>

<h3 id="auto-annotation-via-position-tracking">Auto-annotation via Position Tracking</h3>

<p>The clever bit: Since I control placeholder positions, I can automatically generate NER annotations:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">fill_and_annotate</span><span class="p">(</span><span class="n">template</span><span class="p">,</span> <span class="n">label_vars</span><span class="p">):</span>
    <span class="n">pattern</span> <span class="o">=</span> <span class="n">re</span><span class="p">.</span><span class="nf">compile</span><span class="p">(</span><span class="sa">r</span><span class="sh">'</span><span class="s">&lt;(.*?)&gt;</span><span class="sh">'</span><span class="p">)</span>
    <span class="n">spans</span> <span class="o">=</span> <span class="p">[]</span>
    <span class="n">filled_out</span> <span class="o">=</span> <span class="p">[]</span>
    
    <span class="c1"># Stream-build output &amp; record spans using output positions
</span>    <span class="k">for</span> <span class="n">m</span> <span class="ow">in</span> <span class="n">pattern</span><span class="p">.</span><span class="nf">finditer</span><span class="p">(</span><span class="n">template</span><span class="p">):</span>
        <span class="n">current_len</span> <span class="o">=</span> <span class="nf">sum</span><span class="p">(</span><span class="nf">len</span><span class="p">(</span><span class="n">x</span><span class="p">)</span> <span class="k">for</span> <span class="n">x</span> <span class="ow">in</span> <span class="n">filled_out</span><span class="p">)</span>
        <span class="n">val</span> <span class="o">=</span> <span class="n">replacements</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="n">key</span><span class="p">,</span> <span class="n">m</span><span class="p">.</span><span class="nf">group</span><span class="p">(</span><span class="mi">0</span><span class="p">))</span>
        <span class="n">filled_out</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">val</span><span class="p">)</span>
        
        <span class="k">if</span> <span class="n">key</span> <span class="ow">in</span> <span class="n">replacements</span><span class="p">:</span>
            <span class="n">spans</span><span class="p">.</span><span class="nf">append</span><span class="p">({</span>
                <span class="sh">"</span><span class="s">start</span><span class="sh">"</span><span class="p">:</span> <span class="n">current_len</span><span class="p">,</span>
                <span class="sh">"</span><span class="s">end</span><span class="sh">"</span><span class="p">:</span> <span class="n">current_len</span> <span class="o">+</span> <span class="nf">len</span><span class="p">(</span><span class="n">val</span><span class="p">),</span>
                <span class="sh">"</span><span class="s">text</span><span class="sh">"</span><span class="p">:</span> <span class="n">val</span><span class="p">,</span>
                <span class="sh">"</span><span class="s">labels</span><span class="sh">"</span><span class="p">:</span> <span class="p">[</span><span class="nf">standardize_deid_label</span><span class="p">(</span><span class="n">key</span><span class="p">)]</span>
            <span class="p">})</span>
</code></pre></div></div>

<p>This eliminates manual annotation while preserving character positions for NER training.</p>

<hr />

<h2 id="design-decisions">Design Decisions</h2>

<p><strong>Why not fine-tune an open model locally?</strong> I tried. 7B and 72B models produced repetitive outputs even with temperature tuning. The quality gap between GPT-4 and open models was worth the $30-40 API cost for 9000 samples.</p>

<p><strong>Why not regex everything?</strong> That’s essentially recreating Physionet’s approach. Regex catches “Patient” as PHI, misses context-dependent entities, and fails on procedures named after people.</p>

<p><strong>Why GLiNER-small for iteration?</strong> Training time: minutes not hours. Watching F1 climb from 40% to 75% over 8 iterations required rapid feedback cycles.</p>

<p><strong>When does this approach make sense?</strong> You need proprietary data with privacy constraints. Public datasets alone won’t give you an edge—unless you’ve discovered architectural insight to bake in as inductive bias, you need a data advantage.</p>

<hr />

<h2 id="future-work--open-questions">Future Work &amp; Open Questions</h2>

<ul>
  <li><strong>Dataset diversity metrics</strong>: How diverse is the augmented data really? Haven’t measured unique n-grams, similarity between samples, or entropy of entity distributions.</li>
  <li><strong>Synthetic vs real n-gram analysis</strong>: Need to compare phrase distributions between synthetic radiology reports and real ones to quantify domain shift.</li>
  <li><strong>Quality vs quantity tradeoff</strong>: Would 1000 carefully curated augmented samples outperform 5000 repetitive synthetic ones?</li>
  <li><strong>Inspiration text scaling</strong>: I picked 3200 templates for 9000 samples without formal analysis. Diminishing returns unclear.</li>
  <li><strong>Cross-institutional transfer</strong>: My augmentation uses single-hospital PHI patterns. Generalization unknown.</li>
  <li><strong>Publishing resources</strong>: Need to release filtered mistral-pii dataset on HuggingFace and sanitized code repository.</li>
</ul>

<hr />

<p>The graveyard of dead synthetic datasets taught me this: augmentation isn’t about generating perfect medical text. It’s about preserving what’s real (entity distributions) while varying what’s malleable (context).</p>

<p>Sometimes the best synthetic data is barely synthetic at all.</p>]]></content><author><name>Zardar Khan</name></author><category term="data-augmentation" /><category term="phi-deidentification" /><category term="ner" /><category term="synthetic-data" /><category term="medical-nlp" /><category term="data-quality" /><summary type="html"><![CDATA[Why purely synthetic data failed for medical NER, and how augmenting real PHI with GPT-4 generated context achieved 75% F1 score.]]></summary></entry><entry><title type="html">Road to a SOTA PII Model</title><link href="http://0.0.0.0:4000/2025/06/27/road-to-sota-pii-model.html" rel="alternate" type="text/html" title="Road to a SOTA PII Model" /><published>2025-06-27T00:00:00-04:00</published><updated>2025-06-27T00:00:00-04:00</updated><id>http://0.0.0.0:4000/2025/06/27/road-to-sota-pii-model</id><content type="html" xml:base="http://0.0.0.0:4000/2025/06/27/road-to-sota-pii-model.html"><![CDATA[<p>Yes, I’m using PHI and PII interchangeably. Yes, I know they’re not the same. No, I don’t care. It’s my blog.</p>

<p>The journey started with a test run of a tool from the Gemini group—a Python port of some dusty Perl heuristics from PhysioNet. I liked its ability to swap out PII with fake “surrogate” data. What I didn’t like was it tagging the word <strong>pain</strong> as a person’s name. Context matters. Heuristics alone weren’t going to cut it.</p>

<p>I pulled data from our FHIR store: patient demographics, radiology reports, pathology notes. I queried everything I could—account numbers (probably?), patient and clinician names, DOBs, OHIP/JHN/MRN/CMR identifiers, timestamps, addresses, even terms like “tattoo” (until I found out that was also a medical procedure—thanks for that, oncology).</p>

<p>First attempt: train GLiNER on this data. It’s billed as SOTA for NER, built from Pile spans annotated by GPT, then filtered for PII. I tried finetuning it on 2,000 samples. Results: garbage. Turns out feeding a model endless account numbers with no context doesn’t teach much. Who knew.</p>

<p>So I pivoted. Could I inject context into training by generating full-text sentences around each PII type? I wrote prompts, called the largest LLM I could cram into my 8GB setup (don’t laugh), and started generating. The catch: data can’t leave hospital servers. So now I’m juggling slow inferences, a crowded SLURM cluster, and my dusty desktop GPU.</p>

<p>Validation? Initially fine—my napkin math said we’re good. But when scaled up, the model began repeating itself. Still, I pushed forward. A thousand examples were manually annotated by me, a summer student, and a research assistant. Later discovered span inconsistencies while debugging training. Fantastic. Relabeling time.</p>

<p>Then came a win: someone whispered <strong>ollama</strong> to me. With some coaxing, I got <strong>Qwen3-30B</strong> running locally. Inference takes 20 seconds per call, but the outputs? Gold. I stopped asking LLMs for start/end indices—just get me the text, I’ll regex the rest. Wrote a wrapper to convert spans into NER-ready JSON.</p>

<p>After 7 test batches and dozens of manual checks, Qwen3 delivered near-perfect F1 every time. This means I can automate annotation at scale. Manual review stays in the loop, but I no longer need an army of undergrads to label data.</p>

<p>Next problem: the repetition in augmented data…</p>

<p><em>To be continued.</em></p>]]></content><author><name>Zardar Khan</name></author><category term="nlp" /><category term="ner" /><category term="medical-ai" /><summary type="html"><![CDATA[Yes, I’m using PHI and PII interchangeably. Yes, I know they’re not the same. No, I don’t care. It’s my blog.]]></summary></entry><entry><title type="html">Using Decoder-Only LLMs for PHI De-Identification: A Minimal Setup</title><link href="http://0.0.0.0:4000/2025/06/17/phi-llm-notebook.html" rel="alternate" type="text/html" title="Using Decoder-Only LLMs for PHI De-Identification: A Minimal Setup" /><published>2025-06-17T00:00:00-04:00</published><updated>2025-06-17T00:00:00-04:00</updated><id>http://0.0.0.0:4000/2025/06/17/phi-llm-notebook</id><content type="html" xml:base="http://0.0.0.0:4000/2025/06/17/phi-llm-notebook.html"><![CDATA[<p>This was originally a notebook I built for a summer student. The goal: test if decoder-style LLMs can help extract PHI from clinical text — and wrap their output with simple postprocessing to recover spans and labels in a structured format.</p>

<p>All results below use <strong>Qwen‑2.5‑3B‑Instruct</strong> (≈3B parameters) on a single desktop GPU. Bigger models will likely perform better, but this quick test shows the core idea.</p>

<p>We’ve already fine-tuned several encoder models for this. We also built a baseline using regex and dictionary heuristics. But decoder models like Qwen, trained on broad corpora, offer a chance to extract PHI with very little supervision if we guide them properly.</p>

<h2 id="the-setup">The Setup</h2>

<p>LLMs struggle with exact character positions. Tokenizers don’t operate at character granularity. Asking for <code class="language-plaintext highlighter-rouge">"start"</code> and <code class="language-plaintext highlighter-rouge">"end"</code> positions directly leads to unreliable outputs. So instead, we ask for just this:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>"exact span from input text" [label]
</code></pre></div></div>

<p>Then we match that span against the original input using <code class="language-plaintext highlighter-rouge">re.search()</code> to recover offsets ourselves. This avoids all alignment issues and keeps things clean.</p>

<h2 id="the-prompt">The Prompt</h2>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Extract all PHI (Protected Health Information) entities from the "Content" using the exact text spans as they appear. Follow the steps below:

1. Identify PHI entities in the input text.
2. Assign a label to each entity from this list: hospital, date, month, day, year, holiday, name, person, initials, MRN, medical record number, SIN, social_security_number, social security number, OHIP, identity card number, phone number, address, email, e-mail, email address, fax, location, street, postal code, health insurance id number, health insurance number, insurance number, landline phone number.
3. Return the exact span of the entity from the input, without rephrasing or normalizing.
4. Present the results as a list of strings in JSON format, where each string follows this structure:
   "text span" [label]

Examples:

Content: The patient, MR. JOHN DOE, visited St. VINCENT Hospital on December 12th, 2022. His OHIP: 1234-567-890.
Output: [
  "MR. JOHN DOE" [name],
  "St. VINCENT Hospital" [hospital],
  "December 12th, 2022" [date],
  "OHIP: 1234-567-890" [health insurance number]
]

Content: Contact Dr. LEE-WONG via email: lwong@caremail.org or fax: 555-432-8765. Address: 99 Queen's Blvd., Toronto, ON.
Output: [
  "Dr. LEE-WONG" [name],
  "lwong@caremail.org" [email address],
  "555-432-8765" [fax],
  "99 Queen's Blvd., Toronto, ON" [address]
]

Input: Content: {your clinical text here}
</code></pre></div></div>

<h2 id="real-example-input">Real Example Input</h2>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>LEFT KNEE
Small joint effusion with lipohemarthrosis. Marginal osteophytosis along the lateral compartment. No fracture.
_____________
This report was electronically signed by DONALD, ALPHA, Staff Radiologist on 2010/13/25 at 22:30
</code></pre></div></div>

<h3 id="model-output-verbatim">Model Output (verbatim)</h3>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">[</span><span class="w">
  </span><span class="s2">"LEFT KNEE"</span><span class="w"> </span><span class="p">[</span><span class="err">location</span><span class="p">],</span><span class="w">
  </span><span class="s2">"DONALD, ALPHA"</span><span class="w"> </span><span class="p">[</span><span class="err">name</span><span class="p">],</span><span class="w">
  </span><span class="s2">"2010/13/25"</span><span class="w"> </span><span class="p">[</span><span class="err">date</span><span class="p">],</span><span class="w">
  </span><span class="s2">"22:30"</span><span class="w"> </span><span class="p">[</span><span class="err">time</span><span class="p">]</span><span class="w">
</span><span class="p">]</span><span class="w">
</span></code></pre></div></div>

<h3 id="postprocessed-output">Postprocessed Output</h3>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">[{</span><span class="nl">"start"</span><span class="p">:</span><span class="mi">0</span><span class="p">,</span><span class="nl">"end"</span><span class="p">:</span><span class="mi">9</span><span class="p">,</span><span class="nl">"text"</span><span class="p">:</span><span class="s2">"LEFT KNEE"</span><span class="p">,</span><span class="nl">"labels"</span><span class="p">:[</span><span class="s2">"Location"</span><span class="p">]},</span><span class="w">
 </span><span class="p">{</span><span class="nl">"start"</span><span class="p">:</span><span class="mi">794</span><span class="p">,</span><span class="nl">"end"</span><span class="p">:</span><span class="mi">809</span><span class="p">,</span><span class="nl">"text"</span><span class="p">:</span><span class="s2">"DONALD, ALPHA"</span><span class="p">,</span><span class="nl">"labels"</span><span class="p">:[</span><span class="s2">"Name"</span><span class="p">]},</span><span class="w">
 </span><span class="p">{</span><span class="nl">"start"</span><span class="p">:</span><span class="mi">832</span><span class="p">,</span><span class="nl">"end"</span><span class="p">:</span><span class="mi">842</span><span class="p">,</span><span class="nl">"text"</span><span class="p">:</span><span class="s2">"2010/13/25"</span><span class="p">,</span><span class="nl">"labels"</span><span class="p">:[</span><span class="s2">"Date"</span><span class="p">]},</span><span class="w">
 </span><span class="p">{</span><span class="nl">"start"</span><span class="p">:</span><span class="mi">846</span><span class="p">,</span><span class="nl">"end"</span><span class="p">:</span><span class="mi">851</span><span class="p">,</span><span class="nl">"text"</span><span class="p">:</span><span class="s2">"22:30"</span><span class="p">,</span><span class="nl">"labels"</span><span class="p">:[</span><span class="s2">"time"</span><span class="p">]}]</span><span class="w">
</span></code></pre></div></div>

<p>A few notes:</p>
<ul>
  <li>The model invented a <code class="language-plaintext highlighter-rouge">"time"</code> label. We never gave it one.</li>
  <li>It tagged <code class="language-plaintext highlighter-rouge">"LEFT KNEE"</code> as a <code class="language-plaintext highlighter-rouge">location</code>. Technically true, but not PHI.</li>
  <li><code class="language-plaintext highlighter-rouge">"DONALD, ALPHA"</code> and <code class="language-plaintext highlighter-rouge">"2010/13/25"</code> were both captured exactly and matched correctly.</li>
</ul>

<h2 id="another-example">Another Example</h2>

<p><strong>Input:</strong></p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Dr. CHARMS,LUCKY reviewed the patient's file.
</code></pre></div></div>

<p><strong>Output:</strong></p>
<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">[</span><span class="w">
  </span><span class="s2">"Dr. CHARMS,LUCKY"</span><span class="w"> </span><span class="p">[</span><span class="err">name</span><span class="p">]</span><span class="w">
</span><span class="p">]</span><span class="w">
</span></code></pre></div></div>

<p><strong>Postprocessed:</strong></p>
<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">[{</span><span class="nl">"start"</span><span class="p">:</span><span class="mi">4</span><span class="p">,</span><span class="nl">"end"</span><span class="p">:</span><span class="mi">20</span><span class="p">,</span><span class="nl">"text"</span><span class="p">:</span><span class="s2">"CHARMS,LUCKY"</span><span class="p">,</span><span class="nl">"labels"</span><span class="p">:[</span><span class="s2">"Name"</span><span class="p">]}]</span><span class="w">
</span></code></pre></div></div>

<h2 id="takeaways">Takeaways</h2>

<p>Use decoder LLMs for what they’re good at: labeling spans. Don’t ask them to think in characters. Instead, extract raw spans and reconstruct structure with simple Python logic. This gives you:</p>
<ul>
  <li>Exact offsets</li>
  <li>Clean, valid JSON</li>
  <li>Full control over downstream formatting</li>
</ul>

<p>Even a 3B model can produce usable PHI annotations quickly. The approach still needs deeper evaluation, better prompt tuning, and strict label control, but the core idea works and is light to implement.</p>

<p>If you’re working on clinical de‑identification and want to dig deeper, reach out.</p>]]></content><author><name>Zardar Khan</name></author><category term="nlp" /><category term="llm" /><category term="medical-nlp" /><category term="phi" /><category term="LLM" /><category term="PHI" /><category term="Qwen" /><category term="De-Identification" /><category term="Clinical NLP" /><category term="Jupyter" /><category term="Transformers" /><summary type="html"><![CDATA[This was originally a notebook I built for a summer student. The goal: test if decoder-style LLMs can help extract PHI from clinical text — and wrap their output with simple postprocessing to recover spans and labels in a structured format.]]></summary></entry><entry><title type="html">RAG Experiments: Chunking, Retrieval, Reformulation</title><link href="http://0.0.0.0:4000/2025/06/16/rag-experiments.html" rel="alternate" type="text/html" title="RAG Experiments: Chunking, Retrieval, Reformulation" /><published>2025-06-16T00:00:00-04:00</published><updated>2025-06-16T00:00:00-04:00</updated><id>http://0.0.0.0:4000/2025/06/16/rag-experiments</id><content type="html" xml:base="http://0.0.0.0:4000/2025/06/16/rag-experiments.html"><![CDATA[<p>This post summarizes my experiments building a Retrieval-Augmented Generation (RAG) system over psychiatric discharge documentation. The goal: enable clinicians to ask meaningful, nuanced questions about patients’ post-discharge outlook and care.</p>

<hr />

<h2 id="task">Task</h2>

<p>Given a set of de-identified patient charts (e.g., progress notes, treatment summaries), we want to support natural language queries such as:</p>

<ul>
  <li><em>What sentiment or tone should I approach the patient with?</em></li>
  <li><em>Does the patient have any future plans to look forward to?</em></li>
  <li><em>Has the patient acknowledged the importance of follow-up care?</em></li>
</ul>

<p>These questions require drawing subtle inferences from free-text notes, making structured QA difficult to scale manually.</p>

<hr />

<h2 id="background-embedding-breakdown">Background: Embedding Breakdown</h2>

<p>In earlier work, I experimented with general, biomedical, and radiology-specific embedding models to classify microcalcification status from mammography reports — <em>Positive</em>, <em>Negative</em>, or <em>Not Specified</em>. Despite domain tuning, none of the models captured the specific, sparse signal required for this task.</p>

<blockquote>
  <p>That failure was formative. It showed me that off-the-shelf embedding models often fail to preserve fine-grained clinical meaning — especially when used naïvely in dense retrieval setups.</p>
</blockquote>

<p>This led me to reevaluate how retrieval pipelines are designed: how documents are chunked, how user queries are interpreted, and how semantic drift is corrected.</p>

<hr />

<h2 id="rag-recap-with-notation">RAG Recap (with Notation)</h2>

<p>We want to answer user queries over a corpus <code class="language-plaintext highlighter-rouge">C</code> of clinical documents.</p>

<p><strong>Baseline setup:</strong></p>

<ol>
  <li>Each document <code class="language-plaintext highlighter-rouge">D ∈ C</code> → <code class="language-plaintext highlighter-rouge">M</code> → <code class="language-plaintext highlighter-rouge">D~embedded~</code></li>
  <li>User query <code class="language-plaintext highlighter-rouge">U</code> → <code class="language-plaintext highlighter-rouge">M</code> → <code class="language-plaintext highlighter-rouge">U~embedded~</code></li>
  <li>Compute similarity between <code class="language-plaintext highlighter-rouge">U~embedded~</code> and all <code class="language-plaintext highlighter-rouge">D~embedded~</code> (typically cosine)</li>
  <li>Take top-k matches → feed into an LLM alongside the original query</li>
</ol>

<blockquote>
  <p>This architecture assumes <code class="language-plaintext highlighter-rouge">D~embedded~</code> captures all relevant content in a fixed-length vector. It doesn’t. Compressing complex clinical notes into dense vectors makes it hard to recover granular information — especially when queries are vague or compositional.</p>
</blockquote>

<hr />

<h2 id="chunking-from-naive-to-proposition">Chunking: From Naive to Proposition</h2>

<p>My early chunking attempts included:</p>

<ul>
  <li>Line-by-line splits</li>
  <li>Paragraphs</li>
  <li>Sentence-level tokens</li>
</ul>

<p>None worked well. Small chunks led to hallucinations. Paragraphs introduced noise. Sentence splits broke context mid-thought.</p>

<p>This problem was highlighted in:</p>
<ul>
  <li>Skylar Payne’s <em>RAG Anti-patterns</em></li>
  <li>ChromaDB’s chunking discussion with Anton</li>
</ul>

<p>I pivoted to proposition-based chunking from the paper <em>Dense × Retrieval</em>. Each document was decomposed into standalone, simplified statements:</p>

<blockquote>
  <p>Each proposition is a single idea. Pronouns are replaced. Structure is flattened. Every chunk stands on its own and aligns more naturally with user questions.</p>
</blockquote>

<p>I used GPT-4 to generate these propositions, formatted as JSON. Example prompt:</p>

<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">[</span><span class="w">
  </span><span class="s2">"Jordan M. has been provided with a calendar reminder for the intake appointment."</span><span class="p">,</span><span class="w">
  </span><span class="s2">"Jordan M. expressed motivation to continue structured therapy."</span><span class="p">,</span><span class="w">
  </span><span class="s2">"The outpatient plan is acknowledged by Jordan M. as a good next step."</span><span class="w">
</span><span class="p">]</span><span class="w">
</span></code></pre></div></div>

<hr />

<h2 id="hybrid-retrieval-fixing-similarity-drift">Hybrid Retrieval: Fixing Similarity Drift</h2>

<p>Dense-only retrieval struggled due to mismatched language between user queries and document style.</p>

<p>To fix this, I adopted <strong>hybrid retrieval</strong>:</p>

<ol>
  <li>Use <strong>BM25</strong> to retrieve top-100 candidates based on keyword overlap</li>
  <li>Embed user query + candidate chunks</li>
  <li>Rerank using cosine similarity → return top-k</li>
</ol>

<blockquote>
  <p>BM25 is fast and brittle. Dense models are semantic but noisy. Combining them gave the best of both — lexical recall followed by semantic precision.</p>
</blockquote>

<p>This reduced irrelevant matches and ensured we only embedded promising candidates.</p>

<hr />

<h2 id="query-reformulation">Query Reformulation</h2>

<p>User queries are often vague. Even with proposition chunks, retrieval underperformed.</p>

<p>So I used GPT-4 to rewrite user queries to match the structure of my chunks:</p>

<h3 id="input">Input:</h3>
<blockquote>
  <p>Does the patient have anything they’re looking forward to?</p>
</blockquote>

<h3 id="reformulated">Reformulated:</h3>
<div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">[</span><span class="w">
  </span><span class="s2">"Is the patient looking forward to any specific events?"</span><span class="p">,</span><span class="w">
  </span><span class="s2">"Is the patient anticipating any particular activities?"</span><span class="p">,</span><span class="w">
  </span><span class="s2">"Does the patient have any future plans they are excited about?"</span><span class="w">
</span><span class="p">]</span><span class="w">
</span></code></pre></div></div>

<blockquote>
  <p>Reformulated queries were simpler, explicit, and structurally aligned with the chunk format. This made reranking sharper and generation stronger.</p>
</blockquote>

<hr />

<h2 id="generation-outcomes">Generation Outcomes</h2>

<p>Query: <em>Is the patient looking forward to any specific events?</em></p>

<hr />

<p><strong>🔹 Qwen2.5-7B</strong></p>

<blockquote>
  <p>“Revisiting discharge planning on June 3, 2025.”</p>
</blockquote>

<p>❌ Extracts a date but misses intent. Literal match, low relevance.</p>

<hr />

<p><strong>🔹 Qwen2.5-14B</strong></p>

<blockquote>
  <p>“Jordan M. is looking forward to the intake appointment and is motivated to continue with the structured outpatient therapy plan.”</p>
</blockquote>

<p>✅ Clear answer grounded in chunk content. Matches meaning, not just keywords.</p>

<hr />

<p><strong>🔹 Qwen2.5-32B</strong></p>

<blockquote>
  <p>“Jordan M. is looking forward to the outpatient plan and is motivated to continue with structured therapy, indicating anticipation for these future activities.”</p>
</blockquote>

<p>✅ Strong result with richer phrasing and implied intent. Aligns well with query.</p>

<hr />

<p><strong>🔹 GPT-4</strong></p>

<blockquote>
  <p>“Jordan M. appears to be looking forward to continuing structured therapy and building a routine, as indicated by their motivation and acknowledgment that the outpatient plan is ‘a good next step.’ They have also been provided with a calendar reminder and contact details for an intake appointment, suggesting anticipation of future care.”</p>
</blockquote>

<p>✅ Best synthesis. Integrates multiple signals from chunk content. Demonstrates grounding and inference.</p>

<hr />

<blockquote>
  <p>Prompting for “a short, informative sentence or two” (instead of “just a few words”) made a significant difference. Reformulated queries + reranked proposition chunks enabled smaller models to approach GPT-4-level answers.</p>
</blockquote>

<hr />

<h2 id="next-steps">Next Steps</h2>

<ul>
  <li>Train a FlanT5 propositionizer using GPT-generated examples</li>
  <li>Add filters for patient ID and note timestamp at query time</li>
  <li>Explore chunking beyond propositions: spans, entities, templates</li>
  <li>Evaluate generalizability across clinical QA tasks</li>
</ul>

<hr />

<p>If you’re working on clinical RAG systems and these thoughts hit home, reach out. Let’s discuss how to work together.</p>]]></content><author><name>Zardar Khan</name></author><category term="rag" /><category term="clinical-nlp" /><category term="retrieval" /><category term="chunking" /><category term="query-reformulation" /><category term="hybrid-retrieval" /><summary type="html"><![CDATA[Chunking, hybrid retrieval, and reformulation strategies for clinical RAG pipelines, including Dense X Retrieval and proposition-based methods.]]></summary></entry><entry><title type="html">Building a Bookkeeping Agent: What Worked, What Didn’t</title><link href="http://0.0.0.0:4000/2025/01/19/building-bookkeeping-agent.html" rel="alternate" type="text/html" title="Building a Bookkeeping Agent: What Worked, What Didn’t" /><published>2025-01-19T00:00:00-05:00</published><updated>2025-01-19T00:00:00-05:00</updated><id>http://0.0.0.0:4000/2025/01/19/building-bookkeeping-agent</id><content type="html" xml:base="http://0.0.0.0:4000/2025/01/19/building-bookkeeping-agent.html"><![CDATA[<p>I was interviewing for a role at a company building financial agents. I wanted to understand the patterns by building, not reading. So I built a bookkeeping agent from scratch—tools for parsing bank statements, categorizing transactions, and answering questions about my spending.</p>

<p>The goal: “Process all bank statements and tell me where my money is going.”</p>

<hr />

<h2 id="the-stack">The Stack</h2>

<p>I used <a href="https://github.com/AnswerDotAI/claudette">claudette</a>, a lightweight wrapper around Anthropic’s SDK from Answer.AI. The key feature is <code class="language-plaintext highlighter-rouge">toolloop</code>—it automates the back-and-forth where Claude requests a tool, you execute it, Claude sees the result, requests another tool, and so on until done.</p>

<p>The pattern is simple: define Python functions with type hints and docstrings, pass them to Claude, and let it figure out what to call based on the user’s request.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">get_customer_info</span><span class="p">(</span><span class="n">customer_id</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">dict</span><span class="p">:</span>
    <span class="sh">"</span><span class="s">Retrieves customer information and orders</span><span class="sh">"</span>
    <span class="k">return</span> <span class="n">customers</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="n">customer_id</span><span class="p">,</span> <span class="sh">"</span><span class="s">Customer not found</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div>

<p>Claude uses reflection to inspect the function signature and documentation. You’re essentially giving the LLM a menu of capabilities and letting it compose them.</p>

<hr />

<h2 id="building-up-the-tools">Building Up the Tools</h2>

<h3 id="stage-1-file-io">Stage 1: File I/O</h3>

<p>Started basic. Two tools: <code class="language-plaintext highlighter-rouge">list_files</code> and <code class="language-plaintext highlighter-rouge">read_file</code>.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">list_files</span><span class="p">(</span><span class="n">fp</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">list</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">Given a filepath, returns list of files in it.</span><span class="sh">"""</span>
    <span class="k">return</span> <span class="nc">Path</span><span class="p">(</span><span class="n">fp</span><span class="p">).</span><span class="nf">ls</span><span class="p">()</span>

<span class="k">def</span> <span class="nf">read_file</span><span class="p">(</span><span class="n">fname</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">Read and return the text content of a file.</span><span class="sh">"""</span>
    <span class="k">return</span> <span class="nc">Path</span><span class="p">(</span><span class="n">fname</span><span class="p">).</span><span class="nf">read_text</span><span class="p">()</span>
</code></pre></div></div>

<p>Now the agent can explore the filesystem. Ask “what’s in the data folder?” and it calls <code class="language-plaintext highlighter-rouge">list_files</code>, sees the bank statements, reports back.</p>

<h3 id="stage-2-structured-extraction">Stage 2: Structured Extraction</h3>

<p>Raw text is useless for analysis. I needed structured data.</p>

<p>Built a Pydantic model for statement metadata:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">StatementMetadata</span><span class="p">(</span><span class="n">BaseModel</span><span class="p">):</span>
    <span class="n">source_file</span><span class="p">:</span> <span class="nb">str</span>
    <span class="n">bank_name</span><span class="p">:</span> <span class="nb">str</span>
    <span class="n">account_number</span><span class="p">:</span> <span class="nb">str</span>
    <span class="n">statement_start</span><span class="p">:</span> <span class="nb">str</span>
    <span class="n">statement_end</span><span class="p">:</span> <span class="nb">str</span>
    <span class="n">opening_balance</span><span class="p">:</span> <span class="n">Optional</span><span class="p">[</span><span class="nb">float</span><span class="p">]</span>
    <span class="n">closing_balance</span><span class="p">:</span> <span class="n">Optional</span><span class="p">[</span><span class="nb">float</span><span class="p">]</span>
</code></pre></div></div>

<p>Then a tool that reads a file and extracts structured metadata using Claude’s structured outputs:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">extract_statement_metadata</span><span class="p">(</span><span class="n">filepath</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">dict</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">Extract structured metadata from a bank statement file.</span><span class="sh">"""</span>
    <span class="n">text</span> <span class="o">=</span> <span class="nc">Path</span><span class="p">(</span><span class="n">filepath</span><span class="p">).</span><span class="nf">read_text</span><span class="p">()</span>
    <span class="n">response</span> <span class="o">=</span> <span class="n">anthropic_cli</span><span class="p">.</span><span class="n">beta</span><span class="p">.</span><span class="n">messages</span><span class="p">.</span><span class="nf">parse</span><span class="p">(</span>
        <span class="n">model</span><span class="o">=</span><span class="sh">"</span><span class="s">claude-sonnet-4-5</span><span class="sh">"</span><span class="p">,</span>
        <span class="n">output_format</span><span class="o">=</span><span class="n">StatementMetadata</span><span class="p">,</span>
        <span class="n">messages</span><span class="o">=</span><span class="p">[{</span><span class="sh">"</span><span class="s">role</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">user</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">content</span><span class="sh">"</span><span class="p">:</span> <span class="sa">f</span><span class="sh">"</span><span class="s">Extract metadata:</span><span class="se">\n</span><span class="si">{</span><span class="n">text</span><span class="si">}</span><span class="sh">"</span><span class="p">}]</span>
    <span class="p">)</span>
    <span class="k">return</span> <span class="n">response</span><span class="p">.</span><span class="n">parsed_output</span><span class="p">.</span><span class="nf">model_dump</span><span class="p">()</span>
</code></pre></div></div>

<p>Same pattern for transactions—a <code class="language-plaintext highlighter-rouge">Transaction</code> model with date, description, amount, balance, category.</p>

<h3 id="stage-3-context-window-blowup">Stage 3: Context Window Blowup</h3>

<p>Here’s where it got interesting.</p>

<p>Large bank statements killed the context window. Stuffing 20 pages of transactions into a single extraction call? Disaster.</p>

<p><strong>Fix:</strong> Split by pages first, process each page separately.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">split_by_pages</span><span class="p">(</span><span class="n">text</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> <span class="n">delimiter</span><span class="p">:</span> <span class="nb">str</span> <span class="o">=</span> <span class="sh">'</span><span class="s">--- Page</span><span class="sh">'</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">list</span><span class="p">[</span><span class="nb">str</span><span class="p">]:</span>
    <span class="sh">"""</span><span class="s">Split text into pages based on delimiter.</span><span class="sh">"""</span>
    <span class="n">pages</span> <span class="o">=</span> <span class="n">text</span><span class="p">.</span><span class="nf">split</span><span class="p">(</span><span class="n">delimiter</span><span class="p">)</span>
    <span class="k">return</span> <span class="p">[</span><span class="n">p</span><span class="p">.</span><span class="nf">strip</span><span class="p">()</span> <span class="k">for</span> <span class="n">p</span> <span class="ow">in</span> <span class="n">pages</span> <span class="k">if</span> <span class="n">p</span><span class="p">.</span><span class="nf">strip</span><span class="p">()]</span>
</code></pre></div></div>

<p>Then iterate:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">for</span> <span class="n">i</span><span class="p">,</span> <span class="n">page</span> <span class="ow">in</span> <span class="nf">enumerate</span><span class="p">(</span><span class="n">pages</span><span class="p">):</span>
    <span class="k">if</span> <span class="n">i</span> <span class="o">&gt;</span> <span class="mi">0</span><span class="p">:</span> <span class="n">time</span><span class="p">.</span><span class="nf">sleep</span><span class="p">(</span><span class="mi">2</span><span class="p">)</span>  <span class="c1"># Rate limiting
</span>    <span class="n">response</span> <span class="o">=</span> <span class="n">anthropic_cli</span><span class="p">.</span><span class="n">beta</span><span class="p">.</span><span class="n">messages</span><span class="p">.</span><span class="nf">parse</span><span class="p">(</span>
        <span class="n">output_format</span><span class="o">=</span><span class="n">List</span><span class="p">[</span><span class="n">Transaction</span><span class="p">],</span>
        <span class="n">messages</span><span class="o">=</span><span class="p">[{</span><span class="sh">"</span><span class="s">role</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">user</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">content</span><span class="sh">"</span><span class="p">:</span> <span class="sa">f</span><span class="sh">"</span><span class="s">Extract from page </span><span class="si">{</span><span class="n">i</span><span class="si">}</span><span class="s">:</span><span class="se">\n</span><span class="si">{</span><span class="n">page</span><span class="si">}</span><span class="sh">"</span><span class="p">}]</span>
    <span class="p">)</span>
    <span class="n">all_transactions</span><span class="p">.</span><span class="nf">extend</span><span class="p">(</span><span class="n">response</span><span class="p">.</span><span class="n">parsed_output</span><span class="p">)</span>
</code></pre></div></div>

<p><strong>Key insight:</strong> Don’t return the data to the agent. Save it internally to a CSV and return a summary. Keeps the agent’s context clean.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">df</span><span class="p">.</span><span class="nf">to_csv</span><span class="p">(</span><span class="n">output_path</span><span class="p">,</span> <span class="n">index</span><span class="o">=</span><span class="bp">False</span><span class="p">)</span>
<span class="k">return</span> <span class="sa">f</span><span class="sh">"</span><span class="s">Parsed </span><span class="si">{</span><span class="nf">len</span><span class="p">(</span><span class="n">df</span><span class="p">)</span><span class="si">}</span><span class="s"> transactions. Saved to: </span><span class="si">{</span><span class="n">output_path</span><span class="si">}</span><span class="sh">"</span>
</code></pre></div></div>

<h3 id="stage-4-categorization">Stage 4: Categorization</h3>

<p>Now I had transactions. But “STARBUCKS #12345 TORONTO ON” doesn’t tell me I’m bleeding money on coffee.</p>

<p>Here’s where my day job bled into the side project. In clinical NLP, we work with ontologies like SNOMED—hierarchical structures where a specific diagnosis rolls up into broader categories. Microcalcifications → Breast Findings → Radiology Observations. Parent-child relationships.</p>

<p>I applied the same pattern:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">ParentCategory</span><span class="p">(</span><span class="nb">str</span><span class="p">,</span> <span class="n">Enum</span><span class="p">):</span>
    <span class="n">INCOME</span> <span class="o">=</span> <span class="sh">"</span><span class="s">Income</span><span class="sh">"</span>
    <span class="n">HOUSING</span> <span class="o">=</span> <span class="sh">"</span><span class="s">Housing</span><span class="sh">"</span>
    <span class="n">TRANSPORTATION</span> <span class="o">=</span> <span class="sh">"</span><span class="s">Transportation</span><span class="sh">"</span>
    <span class="n">DINING</span> <span class="o">=</span> <span class="sh">"</span><span class="s">Dining</span><span class="sh">"</span>
    <span class="c1"># ...
</span>
<span class="k">class</span> <span class="nc">TransactionClassification</span><span class="p">(</span><span class="n">BaseModel</span><span class="p">):</span>
    <span class="n">category</span><span class="p">:</span> <span class="n">ParentCategory</span>  <span class="c1"># Constrained enum
</span>    <span class="n">subcategory</span><span class="p">:</span> <span class="nb">str</span>  <span class="c1"># Free-form - let the LLM extract this
</span>    <span class="n">confidence</span><span class="p">:</span> <span class="nb">float</span>
</code></pre></div></div>

<p>The key insight: <strong>constrain the parent, free the child.</strong></p>

<p>Parent categories are a fixed enum—I control the buckets. But <code class="language-plaintext highlighter-rouge">subcategory</code> is a free string. The LLM reads “SHELL GAS STN 4521 TORONTO” and extracts “Shell Gas”. It reads “PETRO CANADA #1234” and extracts “Petro Canada”.</p>

<p>Why does this matter? “Transportation” tells me I spent $400 on getting around. But the subcategory breakdown tells me $280 was Shell and $120 was Petro. Maybe Shell is closer to work but Petro has better prices. That’s actionable.</p>

<p>Same with dining—”Dining: $200” is useless. “Starbucks: $70, McDonald’s: $45, Thai Palace: $85” tells a story.</p>

<p>Let the LLM do what it’s good at: extracting specific merchant names from messy bank descriptions. Constrain what needs consistency (the rollup categories), leave the rest flexible.</p>

<p>Run each transaction through Claude, save the enriched data back to CSV.</p>

<hr />

<h2 id="what-didnt-work-the-calculator">What Didn’t Work: The Calculator</h2>

<p>The agent would say “You spent about $60-70 on Starbucks.” Useful, but not exact.</p>

<p>I built a calculator tool:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">calculate</span><span class="p">(</span><span class="n">expression</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">Safely evaluate a mathematical expression.</span><span class="sh">"""</span>
    <span class="c1"># AST-based safe eval...
</span>    <span class="k">return</span> <span class="sa">f</span><span class="sh">"</span><span class="si">{</span><span class="n">expression</span><span class="si">}</span><span class="s"> = </span><span class="si">{</span><span class="n">result</span><span class="si">}</span><span class="sh">"</span>
</code></pre></div></div>

<p>Added a system prompt: “When reporting totals, ALWAYS use the calculate tool to verify your math.”</p>

<p><strong>Problem:</strong> Transcription errors. The agent would read the CSV, try to construct an expression like <code class="language-plaintext highlighter-rouge">2.57 + 2.89 + 3.10 + ...</code>, but miss values or hallucinate numbers. The math was correct, but the inputs were wrong.</p>

<hr />

<h2 id="what-worked-duckdb">What Worked: DuckDB</h2>

<p>The breakthrough was realizing I could just SQL the CSV directly.</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">duckdb</span>

<span class="n">result</span> <span class="o">=</span> <span class="n">duckdb</span><span class="p">.</span><span class="nf">sql</span><span class="p">(</span><span class="sa">f</span><span class="sh">"""</span><span class="s">
    SELECT SUM(amount)
    FROM </span><span class="sh">'</span><span class="si">{</span><span class="n">filepath</span><span class="si">}</span><span class="sh">'</span><span class="s">
    WHERE subcategory LIKE </span><span class="sh">'</span><span class="s">%Starbucks%</span><span class="sh">'</span><span class="s">
</span><span class="sh">"""</span><span class="p">)</span>
</code></pre></div></div>

<p>Holy smokes. SQL on a CSV. No loading into a database, no setup, just query.</p>

<p>Wrapped it as a tool:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">query_csv</span><span class="p">(</span><span class="n">filepath</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> <span class="n">sql</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">Run a SQL query on a CSV file using DuckDB.</span><span class="sh">"""</span>
    <span class="n">full_sql</span> <span class="o">=</span> <span class="n">sql</span><span class="p">.</span><span class="nf">replace</span><span class="p">(</span><span class="sh">'</span><span class="s">FROM data</span><span class="sh">'</span><span class="p">,</span> <span class="sa">f</span><span class="sh">"</span><span class="s">FROM </span><span class="sh">'</span><span class="si">{</span><span class="n">filepath</span><span class="si">}</span><span class="sh">'"</span><span class="p">)</span>
    <span class="n">result</span> <span class="o">=</span> <span class="n">duckdb</span><span class="p">.</span><span class="nf">sql</span><span class="p">(</span><span class="n">full_sql</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">result</span><span class="p">.</span><span class="nf">df</span><span class="p">().</span><span class="nf">to_markdown</span><span class="p">(</span><span class="n">index</span><span class="o">=</span><span class="bp">False</span><span class="p">)</span>
</code></pre></div></div>

<p>Updated the system prompt: “When reporting totals, ALWAYS use query_csv with SQL to get exact numbers—never estimate.”</p>

<p>Now I could ask:</p>

<ul>
  <li>“How much did I spend on Starbucks?”</li>
  <li>“What’s my biggest expense category?”</li>
  <li>“Show me all dining transactions over $50”</li>
</ul>

<p>And get exact answers backed by real queries.</p>

<hr />

<h2 id="the-payoff">The Payoff</h2>

<p>Ran it on my own bank statements from November-December 2024.</p>

<p>The agent found I was spending <strong>$70/month on Starbucks</strong>.</p>

<p>Two months. $140 on coffee.</p>

<p>I bought a coffee machine on Boxing Day for $40.</p>

<hr />

<h2 id="what-id-do-differently">What I’d Do Differently</h2>

<ol>
  <li>
    <p><strong>Start with DuckDB.</strong> The calculator detour wasted time. SQL-on-CSV is the right abstraction for financial data.</p>
  </li>
  <li>
    <p><strong>Better system prompts earlier.</strong> I kept hitting issues where the agent would explore wrong directories or estimate instead of calculating. Clear constraints in the system prompt (“All files are in X. Never estimate.”) saved debugging time.</p>
  </li>
  <li>
    <p><strong>Batch processing resilience.</strong> Rate limits hit hard during categorization. Built in sleep/retry, but should have added checkpointing—resume from where you left off.</p>
  </li>
</ol>

<hr />

<h2 id="code">Code</h2>

<p>The full notebook is here: <a href="https://colab.research.google.com/gist/khankanz/02d572ad56a40f10771a87abd9decddb/build-an-ai-agent-from-scratch.ipynb">Colab</a></p>

<p>If you’re learning agent patterns, I’d recommend building something similar on your own data. The patterns become obvious when you hit the walls yourself.</p>]]></content><author><name>Zardar Khan</name></author><category term="agents" /><category term="tool-use" /><category term="claude" /><category term="duckdb" /><category term="pydantic" /><category term="structured-extraction" /><summary type="html"><![CDATA[I built tools for a financial agent from scratch while interviewing for an agents role. Calculator failed, DuckDB saved the day, and I found my $70/month Starbucks habit.]]></summary></entry></feed>